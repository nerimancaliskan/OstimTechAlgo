// Bir dizi verildiğinde, dizideki tüm elemanları ters çevirin (reverse).
/// çözüm yöntemim : kullanıcıdan girdi alıp arrayin içine yazılan ve  ne kadar size var onu saptayıp sonrasında o size kadar for döngüsü olacak ve bu for döngüsü first in last out şeklinde yazılacak yani arrayin 0. indeksi size 5 ise 4. indekse sahip olacak 

#include < stdio.h> 

int main(){
    int size ;
    printf("Please enter the size of array : ");
    scanf("%d", &size);
    int arr [size] ;

}